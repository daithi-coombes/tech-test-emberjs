## Customize the test machine
machine:

  # Add some environment variables
  environment:
    CIRCLE_ENV: test
    NODE_ENV: test

## Customize deployment commands
deployment:
  production:
    branch: master
    commands:
      - ssh -p 7826 circleci@137.74.112.216 'sh ~/tech-test-emberjs/vendor/deploy_master.sh'
