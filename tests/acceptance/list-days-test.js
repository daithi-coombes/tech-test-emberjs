import { test } from 'qunit';
import moduleForAcceptance from 'tech-test/tests/helpers/module-for-acceptance';

moduleForAcceptance('Acceptance | list days');

test('should show graphic data for current city', function(assert){
  assert.equal(true, true);
});
/*
test('should show date and textual data', function(assert){
});

test('should list 16 days after today, with basic info', function(assert){
});

test('day tile should link to single day', function(assert){
});
*/
